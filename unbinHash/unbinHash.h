#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#ifndef UNBINHASH_H
#define UNBINHASH_H
  char *unbinHash_hashString(const char*);
  char *unbinHash_hashFile(const char*);
  void unbinHash_displayString(const char*);
#endif
